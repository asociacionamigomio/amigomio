# Plan de implementación — Fase 0: cimientos

> **Para quien ejecute esto:** usar `superpowers:subagent-driven-development` (recomendado) o
> `superpowers:executing-plans`, tarea a tarea. Los pasos llevan casilla (`- [ ]`) para ir marcando.

**Objetivo:** dejar funcionando el alta de clientes y las fichas de perro con su cuestionario y
sus fechas sanitarias, con el chip y el nombre protegidos por la base de datos, y el panel de
administración de clientes. **Todavía no se reserva nada.**

**Arquitectura:** PWA sin framework ni compilación —lo que hay en el repositorio es lo que se
sirve— sobre Supabase (Postgres, Auth con correo y contraseña, Storage y Row Level Security).
Las reglas viven en la base de datos, no en el navegador. Alojamiento en GitHub Pages.

**Pila:** HTML + CSS + JavaScript (módulos ES, sin dependencias) · Supabase · pruebas con
`node --test`, sin framework de test.

**Diseño que implementa:** `2026-09-12-amigomio-reservas-design.md` (mismo repositorio).

**Dónde vive este plan:** en la raíz, no en `docs/superpowers/plans/`, porque el repositorio se
alimenta por ahora desde el navegador y las carpetas complican la subida. Cuando haya acceso por
terminal se recolocan spec y plan juntos.

## Restricciones globales

Copiadas literalmente del diseño. **Se aplican a todas las tareas.**

- **Sin paso de compilación.** Nada de npm install para que la web funcione. Las únicas
  dependencias de `package.json` son para las pruebas, y hoy no hay ninguna.
- **Sin secretos en el repositorio.** La `anon key` de Supabase es pública por diseño. La
  `service_role` no entra nunca. Tampoco el IBAN, ni teléfonos personales, ni nombres de terceros:
  **el repositorio es público**.
- **Las reglas se protegen en la base de datos**, con RLS y triggers, no con comprobaciones del
  navegador.
- **Idioma: español**, con acentos correctos, en la interfaz, en los comentarios y en los nombres
  de tablas y columnas.
- **Tono amigable y familiar** en todo texto que vea el cliente. «Nos falta saber cuánto pesa
  Luna», no «Error: campo obligatorio».
- **Marca:** azul principal `#4E80A5`, azul medio `#6199C8`, azul claro `#79BAE4`, amarillo
  `#F6EC4D`, rojo `#C32927`. Tipografía **Poppins**.
- **Chip y nombre del perro son inmutables para el propietario.** Sólo cambian por solicitud
  validada por administración.
- **Peso y celo NO van en la ficha del perro.** Se preguntan en cada reserva (fase 2).

---

## Antes de empezar: lo que tiene que hacer Santiago

Esto no lo puede hacer el programador ni Claude: hace falta crear una cuenta, y eso lo haces tú.

1. Entra en <https://supabase.com> y crea una cuenta (o entra con la que ya tienes del CEPPB).
2. **New project**. Nombre: `amigomio`. Región: **West EU (Ireland)** o **Central EU (Frankfurt)**.
3. Te pedirá una contraseña para la base de datos. Genera una larga, guárdala en tu gestor de
   contraseñas y **no la escribas en ningún fichero del proyecto**.
4. Cuando termine de crearse, ve a **Project Settings → API** y copia dos cosas:
   - **Project URL** (algo como `https://abcdefgh.supabase.co`)
   - **anon public** (una clave larga que empieza por `sb_publishable_` o `eyJ`)
5. Pégamelas en el chat. Esas dos **sí** van dentro del repositorio: son públicas por diseño,
   igual que en el CEPPB.
6. En **Authentication → Sign In / Providers → Email**, deja activado *Confirm email*. Es lo que
   obliga a verificar el correo antes de poder reservar.

Sin el punto 5 se pueden hacer las tareas 1, 3 y 4 (son lógica pura y no tocan Supabase), pero
no las demás.

---

## Estructura de ficheros

| Fichero | De qué se encarga |
|---|---|
| `index.html` | La aplicación entera, una sola página |
| `css/estilo.css` | Marca: colores, Poppins, componentes |
| `js/config.js` | URL y clave pública de Supabase |
| `js/util.js` | Fechas, textos, ayudas sin dependencias |
| `js/perro.js` | **Lógica pura** del perro: chip, edad, categoría |
| `js/sanidad.js` | **Lógica pura**: vigencia de vacunas y desparasitaciones |
| `js/sesion.js` | Alta, entrada con correo y contraseña, verificación |
| `js/datos.js` | Todo lo que habla con Supabase |
| `js/formularios.js` | El alta por pasos, con guardado a medias |
| `js/vistas/*.js` | Una por pantalla |
| `js/app.js` | Arranque y navegación |
| `db/schema.sql` | Tablas, RLS y triggers |
| `db/storage.sql` | Cubos de Storage: fotos y cartillas |
| `pruebas/*.test.mjs` | Las pruebas |

**Por qué `perro.js` y `sanidad.js` están separados de todo lo demás:** no tocan ni el navegador
ni la base de datos. Son funciones que reciben datos y devuelven datos, así que se prueban de
verdad, sin simular nada. Ahí es donde viven las reglas que más caro sale equivocar.

---

## Tarea 1: Esqueleto del repositorio y primera prueba

**Ficheros:**
- Crear: `package.json`, `CLAUDE.md`, `index.html`, `css/estilo.css`, `manifest.webmanifest`, `sw.js`
- Probar: `pruebas/marca.test.mjs`

**Interfaces:**
- Consume: nada, es el principio
- Produce: `npm test` y `npm run servir` funcionando; variables CSS `--azul`, `--azul-medio`,
  `--azul-claro`, `--amarillo`, `--rojo`

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/marca.test.mjs`:

```js
/* ============================================================
   Los colores de la marca salen del logo de AmigoMío, medidos
   sobre el PNG original. Si alguien los cambia "a ojo", la app
   deja de parecerse a la casa. Esta prueba los clava.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const lee = f => readFileSync(new URL("../" + f, import.meta.url), "utf8");

const COLORES = {
  "--azul":       "#4E80A5",
  "--azul-medio": "#6199C8",
  "--azul-claro": "#79BAE4",
  "--amarillo":   "#F6EC4D",
  "--rojo":       "#C32927",
};

test("el CSS define los cinco colores de la marca", () => {
  const css = lee("css/estilo.css");
  for (const [variable, valor] of Object.entries(COLORES)) {
    const re = new RegExp(variable + "\\s*:\\s*" + valor, "i");
    assert.match(css, re, `falta ${variable}: ${valor}`);
  }
});

test("la tipografía es Poppins", () => {
  const css = lee("css/estilo.css");
  assert.match(css, /Poppins/i, "la marca usa Poppins");
});

test("index.html no carga ningún framework ni dependencia compilada", () => {
  const html = lee("index.html");
  assert.doesNotMatch(html, /react|vue|angular|node_modules|\/dist\//i,
    "esta app se sirve tal cual: sin framework y sin compilar");
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `cd /Users/santi/amigomio && node --test "pruebas/*.test.mjs"`
Esperado: FALLA, porque no existe `css/estilo.css`.

- [ ] **Paso 3: crear `package.json`**

```json
{
  "name": "amigomio",
  "private": true,
  "description": "App de reservas de la residencia canina AmigoMío",
  "scripts": {
    "test": "node --test \"pruebas/*.test.mjs\"",
    "servir": "python3 -m http.server 8777"
  }
}
```

- [ ] **Paso 4: crear `css/estilo.css`**

```css
/* ============================================================
   Marca de AmigoMío.

   Los cinco colores están medidos sobre el logo original
   (PNG 1808x1571): el azul es el 55 % de la imagen, y el
   amarillo y el rojo son las rayas de la estela.
   ============================================================ */
@import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap");

:root {
  --azul:       #4E80A5;
  --azul-medio: #6199C8;
  --azul-claro: #79BAE4;
  --amarillo:   #F6EC4D;
  --rojo:       #C32927;

  --texto:      #223039;
  --texto-flojo:#5C6B76;
  --fondo:      #F7FAFC;
  --tarjeta:    #FFFFFF;
  --borde:      #DCE6ED;

  --radio: 14px;
  --sombra: 0 1px 3px rgba(34, 48, 57, .08), 0 6px 18px rgba(34, 48, 57, .06);
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: "Poppins", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  font-weight: 400;
  color: var(--texto);
  background: var(--fondo);
  line-height: 1.55;
}

h1, h2, h3 { font-weight: 600; line-height: 1.25; margin: 0 0 .5rem; }

.tarjeta {
  background: var(--tarjeta);
  border: 1px solid var(--borde);
  border-radius: var(--radio);
  box-shadow: var(--sombra);
  padding: 1.25rem;
}

.boton {
  font: inherit;
  font-weight: 600;
  border: 0;
  border-radius: 999px;
  padding: .7rem 1.4rem;
  background: var(--azul);
  color: #fff;
  cursor: pointer;
}
.boton:hover { background: var(--azul-medio); }
.boton[disabled] { opacity: .5; cursor: not-allowed; }

.aviso  { border-left: 4px solid var(--amarillo); padding: .75rem 1rem; background: #FFFDF0; }
.error  { border-left: 4px solid var(--rojo);     padding: .75rem 1rem; background: #FDF2F2; }

/* El móvil manda: casi todo el uso será desde el teléfono. */
.contenedor { max-width: 760px; margin: 0 auto; padding: 1rem; }
```

- [ ] **Paso 5: crear `index.html`**

```html
<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>AmigoMío — Residencia canina</title>
  <meta name="theme-color" content="#4E80A5">
  <link rel="manifest" href="manifest.webmanifest">
  <link rel="stylesheet" href="css/estilo.css">
</head>
<body>
  <div class="contenedor" id="app">
    <h1>AmigoMío</h1>
    <p>Cargando…</p>
  </div>
  <script src="js/config.js"></script>
  <script type="module" src="js/app.js"></script>
</body>
</html>
```

- [ ] **Paso 6: crear `manifest.webmanifest` y `sw.js`**

`manifest.webmanifest`:

```json
{
  "name": "AmigoMío — Residencia canina",
  "short_name": "AmigoMío",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#F7FAFC",
  "theme_color": "#4E80A5",
  "lang": "es",
  "icons": []
}
```

`sw.js`:

```js
/* De momento no cachea nada. Está para que la PWA sea instalable;
   la caché llega en la fase 2, cuando haya pantallas que merezca
   la pena tener disponibles sin cobertura. */
self.addEventListener("install", () => self.skipWaiting());
self.addEventListener("activate", e => e.waitUntil(self.clients.claim()));
```

- [ ] **Paso 7: crear `js/config.js` y `js/app.js` mínimos**

`js/config.js`:

```js
/* ============================================================
   Conexión con Supabase.

   Estos dos datos NO son contraseñas: Supabase los llama públicos
   por diseño y van dentro de la página. Lo que protege AmigoMío
   son las políticas de la base de datos (db/schema.sql).

   La clave `service_role` no se escribe aquí jamás.

   Los sacas de: Project Settings > API
   ============================================================ */
window.CONFIG = {
  SUPABASE_URL:  "PENDIENTE",
  SUPABASE_ANON: "PENDIENTE",
};

CONFIG.configurado = CONFIG.SUPABASE_URL !== "PENDIENTE" &&
                     CONFIG.SUPABASE_ANON !== "PENDIENTE";
```

`js/app.js`:

```js
const app = document.getElementById("app");

if (!window.CONFIG?.configurado) {
  app.innerHTML = `
    <h1>AmigoMío</h1>
    <div class="aviso">
      Falta conectar la base de datos. Rellena <code>js/config.js</code>
      con los datos de Supabase.
    </div>`;
} else {
  app.innerHTML = `<h1>AmigoMío</h1><p>Conectado. Todavía sin pantallas.</p>`;
}

if ("serviceWorker" in navigator) navigator.serviceWorker.register("sw.js");
```

- [ ] **Paso 8: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test "pruebas/*.test.mjs"`
Esperado: PASAN las tres.

- [ ] **Paso 9: comprobarlo en el navegador**

Ejecutar: `npm run servir` y abrir <http://localhost:8777>
Esperado: se ve «AmigoMío» y el aviso amarillo de que falta conectar la base de datos.

- [ ] **Paso 10: commit**

```bash
git add package.json CLAUDE.md index.html css/ js/ manifest.webmanifest sw.js pruebas/
git commit -m "Esqueleto de la PWA con la marca de AmigoMío"
```

---

## Tarea 2: Lógica pura del perro

**Ficheros:**
- Crear: `js/perro.js`
- Probar: `pruebas/perro.test.mjs`

**Interfaces:**
- Consume: nada
- Produce:
  - `chipValido(chip) -> boolean`
  - `normalizarChip(chip) -> string` (quita espacios y guiones)
  - `edadEnMeses(fechaNacimiento, hoy) -> number`
  - `esCachorro(fechaNacimiento, hoy) -> boolean` (menos de 6 meses)
  - `necesitaAlojamientoEspecial(perro) -> boolean`
  - `puedenCompartir(perroA, perroB) -> { si: boolean, motivo: string }`

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/perro.test.mjs`:

```js
/* ============================================================
   Reglas del perro que no se pueden equivocar:

   - El chip es el identificador de verdad. Si se cuela uno mal
     formado, el mismo perro acaba dado de alta dos veces.
   - Un perro agresivo con personas SOLO puede ir a uno de los dos
     alojamientos especiales, y siempre solo. Si esta función
     falla, se mete a alguien donde no debe.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { chipValido, normalizarChip, edadEnMeses, esCachorro,
         necesitaAlojamientoEspecial, puedenCompartir } from "../js/perro.js";

test("el chip son 15 dígitos", () => {
  assert.equal(chipValido("941000012345678"), true);
  assert.equal(chipValido("94100001234567"), false, "14 dígitos no valen");
  assert.equal(chipValido("9410000123456789"), false, "16 tampoco");
  assert.equal(chipValido("94100001234567X"), false, "letras no");
  assert.equal(chipValido(""), false);
  assert.equal(chipValido(null), false);
});

test("el chip se normaliza antes de comprobarlo", () => {
  assert.equal(normalizarChip(" 941 0000 1234 5678 "), "941000012345678");
  assert.equal(normalizarChip("941-0000-1234-5678"), "941000012345678");
  assert.equal(chipValido("941 0000 1234 5678"), true);
});

test("la edad se cuenta en meses cumplidos", () => {
  assert.equal(edadEnMeses("2026-01-15", "2026-09-12"), 7);
  assert.equal(edadEnMeses("2026-09-13", "2026-09-12"), 0, "aún no ha nacido: 0, no negativo");
  assert.equal(edadEnMeses("2020-09-12", "2026-09-12"), 72);
});

test("es cachorro por debajo de 6 meses", () => {
  assert.equal(esCachorro("2026-05-01", "2026-09-12"), true);
  assert.equal(esCachorro("2026-01-01", "2026-09-12"), false);
});

test("agresivo con personas exige alojamiento especial", () => {
  assert.equal(necesitaAlojamientoEspecial({ agresivoConPersonas: true }), true);
  assert.equal(necesitaAlojamientoEspecial({ agresivoConPersonas: false }), false);
  assert.equal(necesitaAlojamientoEspecial({}), false);
});

test("un agresivo con personas nunca comparte", () => {
  const bravo = { nombre: "Bravo", sexo: "macho", agresivoConPersonas: true, sociable: "todos" };
  const luna  = { nombre: "Luna",  sexo: "hembra", agresivoConPersonas: false, sociable: "todos" };
  const r = puedenCompartir(bravo, luna);
  assert.equal(r.si, false);
  assert.match(r.motivo, /Bravo/, "el motivo tiene que decir de qué perro se trata");
});

test("la sociabilidad manda sobre quién comparte", () => {
  const soloMachos = { nombre: "Toby", sexo: "macho", sociable: "machos", agresivoConPersonas: false };
  const hembra     = { nombre: "Nala", sexo: "hembra", sociable: "todos", agresivoConPersonas: false };
  const macho      = { nombre: "Kiba", sexo: "macho",  sociable: "todos", agresivoConPersonas: false };
  assert.equal(puedenCompartir(soloMachos, hembra).si, false);
  assert.equal(puedenCompartir(soloMachos, macho).si, true);
});

test("el que no se lleva con ninguno, con ninguno", () => {
  const arisco = { nombre: "Rocky", sexo: "macho", sociable: "ninguno", agresivoConPersonas: false };
  const majo   = { nombre: "Kira",  sexo: "hembra", sociable: "todos",  agresivoConPersonas: false };
  assert.equal(puedenCompartir(arisco, majo).si, false);
  assert.equal(puedenCompartir(majo, arisco).si, false, "la regla vale en los dos sentidos");
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/perro.test.mjs`
Esperado: FALLA con «Cannot find module '../js/perro.js'».

- [ ] **Paso 3: escribir `js/perro.js`**

```js
/* ============================================================
   Reglas del perro. Funciones puras: entran datos, salen datos.
   Ni tocan el navegador ni la base de datos, para poder probarlas
   de verdad y no a base de simulacros.
   ============================================================ */

/** Quita espacios y guiones: la gente copia el chip de la cartilla como puede. */
export function normalizarChip(chip) {
  if (typeof chip !== "string") return "";
  return chip.replace(/[\s-]/g, "");
}

/** Los microchips ISO son 15 dígitos. Ni 14 ni 16, y sin letras. */
export function chipValido(chip) {
  return /^\d{15}$/.test(normalizarChip(chip));
}

/** Meses cumplidos. Si la fecha es futura devuelve 0, nunca negativo. */
export function edadEnMeses(fechaNacimiento, hoy) {
  const n = new Date(fechaNacimiento + "T00:00:00");
  const h = new Date(hoy + "T00:00:00");
  if (isNaN(n) || isNaN(h) || n > h) return 0;
  let meses = (h.getFullYear() - n.getFullYear()) * 12 + (h.getMonth() - n.getMonth());
  if (h.getDate() < n.getDate()) meses--;
  return Math.max(0, meses);
}

/** Cachorro hasta los 6 meses: el programa sanitario los aloja aparte. */
export function esCachorro(fechaNacimiento, hoy) {
  return edadEnMeses(fechaNacimiento, hoy) < 6;
}

/** Agresivo con personas: sólo alojamiento especial, y siempre solo. */
export function necesitaAlojamientoEspecial(perro) {
  return perro?.agresivoConPersonas === true;
}

/**
 * ¿Pueden ir estos dos al mismo alojamiento?
 * Devuelve el motivo para poder enseñárselo al cliente tal cual.
 */
export function puedenCompartir(a, b) {
  if (necesitaAlojamientoEspecial(a))
    return { si: false, motivo: `${a.nombre} necesita alojamiento propio y no puede compartir.` };
  if (necesitaAlojamientoEspecial(b))
    return { si: false, motivo: `${b.nombre} necesita alojamiento propio y no puede compartir.` };

  const acepta = (uno, otro) => {
    if (uno.sociable === "ninguno") return `${uno.nombre} está mejor solo.`;
    if (uno.sociable === "machos"  && otro.sexo !== "macho")
      return `${uno.nombre} solo se lleva bien con machos.`;
    if (uno.sociable === "hembras" && otro.sexo !== "hembra")
      return `${uno.nombre} solo se lleva bien con hembras.`;
    return null;
  };

  const motivo = acepta(a, b) || acepta(b, a);
  return motivo ? { si: false, motivo } : { si: true, motivo: "" };
}
```

- [ ] **Paso 4: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/perro.test.mjs`
Esperado: PASAN las siete.

- [ ] **Paso 5: commit**

```bash
git add js/perro.js pruebas/perro.test.mjs
git commit -m "Reglas del perro: chip, edad y con quién puede compartir"
```

---

## Tarea 3: Lógica sanitaria

Ésta es la tarea con más reglas escondidas del plan. Léela entera antes de empezar.

**Ficheros:**
- Crear: `js/sanidad.js`
- Probar: `pruebas/sanidad.test.mjs`

**Interfaces:**
- Consume: nada
- Produce:
  - `REQUISITOS` — el catálogo, un objeto por requisito
  - `estadoRequisito({requisito, registro, entrada, salida}) -> {estado, caduca, mensaje}`
    donde `estado` es uno de `"al-dia"`, `"caduca-durante"`, `"caducado"`, `"demasiado-reciente"`, `"sin-datos"`
  - `revisarPerro(perro, entrada, salida) -> {apto: boolean, problemas: [...]}`

**Las dos formas de caducar, que no son la misma:**

1. **Vacunas** — se ponen y valen un tiempo. Caducan *después*. La rabia puesta el 1/3/2026 vale
   hasta el 1/3/2027.
2. **Desparasitación interna** — tiene que ser **reciente**: dentro de los 30 días *anteriores* a
   la entrada. Una desparasitación de hace un año no vale aunque «no haya caducado».

Y encima hay un tercer caso: la **tos de las perreras** exige estar puesta **al menos 15 días
antes** de entrar. Si se la ponen anteayer, no vale todavía. Igual las primovacunaciones, a 21 días.

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/sanidad.test.mjs`:

```js
/* ============================================================
   Vigencia sanitaria, tal y como la exige el Programa de manejo,
   higiene y profilaxis del núcleo zoológico.

   Lo que hay que comprobar NO es si está al día hoy: es si lo
   estará durante la estancia. Un perro que entra el 7 de agosto
   y sale el 11 con la leptospirosis venciendo el 9 no vale,
   aunque hoy, 12 de septiembre, todo parezca correcto.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { REQUISITOS, estadoRequisito, revisarPerro } from "../js/sanidad.js";

const rabia = REQUISITOS.find(r => r.id === "rabia");
const tos   = REQUISITOS.find(r => r.id === "traqueobronquitis");
const desp  = REQUISITOS.find(r => r.id === "desparasitacion_interna");

test("el catálogo trae los requisitos del programa sanitario", () => {
  const ids = REQUISITOS.map(r => r.id);
  for (const id of ["rabia", "polivalente", "leptospirosis", "traqueobronquitis",
                    "desparasitacion_interna", "antiparasitario_externo", "leishmaniosis"]) {
    assert.ok(ids.includes(id), `falta el requisito ${id}`);
  }
  assert.equal(REQUISITOS.find(r => r.id === "leishmaniosis").obligatorio, false,
    "la leishmaniosis se recomienda, no se exige");
});

test("sin fecha, no hay nada que comprobar", () => {
  const r = estadoRequisito({ requisito: rabia, registro: null,
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "sin-datos");
});

test("una vacuna en vigor durante toda la estancia está al día", () => {
  const r = estadoRequisito({ requisito: rabia, registro: { fecha: "2026-03-01" },
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "al-dia");
  assert.equal(r.caduca, "2027-03-01");
});

test("si vence en mitad de la estancia, avisa con la fecha", () => {
  const r = estadoRequisito({ requisito: rabia, registro: { fecha: "2025-08-09" },
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "caduca-durante");
  assert.equal(r.caduca, "2026-08-09");
  assert.match(r.mensaje, /9 de agosto/, "el mensaje tiene que decir la fecha en cristiano");
});

test("si venció antes de entrar, está caducada", () => {
  const r = estadoRequisito({ requisito: rabia, registro: { fecha: "2025-01-01" },
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "caducado");
});

test("manda la fecha de caducidad escrita a mano si la hay", () => {
  /* El collar antiparasitario dura distinto que la pipeta: si el
     propietario apunta hasta cuándo vale, eso es lo que cuenta. */
  const r = estadoRequisito({ requisito: rabia,
                              registro: { fecha: "2025-01-01", validoHasta: "2026-12-31" },
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "al-dia");
  assert.equal(r.caduca, "2026-12-31");
});

test("la tos de las perreras necesita 15 días de margen", () => {
  const justa = estadoRequisito({ requisito: tos, registro: { fecha: "2026-07-23" },
                                  entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(justa.estado, "al-dia", "15 días exactos valen");

  const tarde = estadoRequisito({ requisito: tos, registro: { fecha: "2026-08-01" },
                                  entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(tarde.estado, "demasiado-reciente");
  assert.match(tarde.mensaje, /15 días/);
});

test("una primovacunación necesita 21 días", () => {
  const r = estadoRequisito({ requisito: rabia,
                              registro: { fecha: "2026-08-01", primovacunacion: true },
                              entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(r.estado, "demasiado-reciente");
  assert.match(r.mensaje, /21 días/);
});

test("la desparasitación interna tiene que ser de los 30 días anteriores", () => {
  const reciente = estadoRequisito({ requisito: desp, registro: { fecha: "2026-07-20" },
                                     entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(reciente.estado, "al-dia");

  const vieja = estadoRequisito({ requisito: desp, registro: { fecha: "2026-06-01" },
                                  entrada: "2026-08-07", salida: "2026-08-11" });
  assert.equal(vieja.estado, "caducado");
  assert.match(vieja.mensaje, /30 días/);
});

test("revisar el perro entero devuelve sólo lo que falla", () => {
  const perro = {
    nombre: "Luna",
    sanidad: {
      rabia:                   { fecha: "2026-03-01" },
      polivalente:             { fecha: "2026-03-01" },
      leptospirosis:           { fecha: "2025-08-09" },
      traqueobronquitis:       { fecha: "2026-06-01" },
      desparasitacion_interna: { fecha: "2026-07-20" },
      antiparasitario_externo: { fecha: "2026-08-01" },
    },
  };
  const r = revisarPerro(perro, "2026-08-07", "2026-08-11");
  assert.equal(r.apto, false);
  assert.equal(r.problemas.length, 1, "solo falla la leptospirosis");
  assert.equal(r.problemas[0].id, "leptospirosis");
  assert.match(r.problemas[0].mensaje, /Luna/, "el mensaje nombra al perro");
});

test("un perro con todo en regla es apto", () => {
  const perro = {
    nombre: "Toby",
    sanidad: {
      rabia:                   { fecha: "2026-03-01" },
      polivalente:             { fecha: "2026-03-01" },
      leptospirosis:           { fecha: "2026-03-01" },
      traqueobronquitis:       { fecha: "2026-06-01" },
      desparasitacion_interna: { fecha: "2026-07-20" },
      antiparasitario_externo: { fecha: "2026-08-01" },
    },
  };
  const r = revisarPerro(perro, "2026-08-07", "2026-08-11");
  assert.equal(r.apto, true);
  assert.equal(r.problemas.length, 0);
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/sanidad.test.mjs`
Esperado: FALLA con «Cannot find module '../js/sanidad.js'».

- [ ] **Paso 3: escribir `js/sanidad.js`**

```js
/* ============================================================
   Vigencia sanitaria. Sale del Programa de manejo, higiene y
   profilaxis del núcleo zoológico, de 01/09/2026.

   Tres formas distintas de no valer, y conviene no confundirlas:

   1. CADUCA DESPUÉS  — las vacunas. Se ponen y valen un tiempo.
   2. TIENE QUE SER RECIENTE — la desparasitación interna: dentro
      de los 30 días anteriores a la entrada. Una de hace un año
      no vale aunque nadie la haya "caducado".
   3. NECESITA MARGEN — la tos de las perreras pide 15 días desde
      que se pone; las primovacunaciones, 21. Puesta anteayer,
      todavía no protege.

   Y lo que se comprueba NO es si está al día hoy, sino si lo
   estará durante la estancia.
   ============================================================ */

const DIA = 86400000;
const MESES = ["enero","febrero","marzo","abril","mayo","junio","julio",
               "agosto","septiembre","octubre","noviembre","diciembre"];

const aFecha = s => new Date(s + "T00:00:00");
const aTexto = d => d.toISOString().slice(0, 10);

/** "2026-08-09" -> "9 de agosto de 2026", para poder enseñarlo tal cual. */
export function enCristiano(iso) {
  const d = aFecha(iso);
  return `${d.getDate()} de ${MESES[d.getMonth()]} de ${d.getFullYear()}`;
}

function sumarMeses(iso, meses) {
  const d = aFecha(iso);
  const dia = d.getDate();
  d.setMonth(d.getMonth() + meses);
  if (d.getDate() < dia) d.setDate(0); // 31 de enero + 1 mes = 28/29 de febrero
  return aTexto(d);
}

export const REQUISITOS = [
  { id: "rabia", nombre: "Rabia",
    vigenciaMeses: 12, obligatorio: true },

  { id: "polivalente", nombre: "Polivalente (moquillo, parvovirosis, hepatitis y parainfluenza)",
    vigenciaMeses: 12, obligatorio: true },

  { id: "leptospirosis", nombre: "Leptospirosis",
    vigenciaMeses: 12, obligatorio: true },

  { id: "traqueobronquitis", nombre: "Tos de las perreras",
    vigenciaMeses: 12, diasMinimosAntes: 15, obligatorio: true },

  { id: "desparasitacion_interna", nombre: "Desparasitación interna",
    maximoDiasAntes: 30, obligatorio: true },

  { id: "antiparasitario_externo", nombre: "Antiparasitario externo",
    vigenciaMeses: 1, obligatorio: true },

  { id: "leishmaniosis", nombre: "Leishmaniosis",
    vigenciaMeses: 12, obligatorio: false },
];

/** Días de margen que exige este registro: 21 si es primovacunación. */
function margenExigido(requisito, registro) {
  if (registro.primovacunacion) return 21;
  return requisito.diasMinimosAntes || 0;
}

export function estadoRequisito({ requisito, registro, entrada, salida }) {
  if (!registro || !registro.fecha)
    return { estado: "sin-datos", caduca: null,
             mensaje: `Nos falta la fecha de ${requisito.nombre.toLowerCase()}.` };

  const puesta = aFecha(registro.fecha);
  const dEntrada = aFecha(entrada);

  /* 3. ¿Le ha dado tiempo a hacer efecto? */
  const margen = margenExigido(requisito, registro);
  if (margen > 0 && (dEntrada - puesta) / DIA < margen) {
    return { estado: "demasiado-reciente", caduca: null,
             mensaje: `${requisito.nombre} tiene que estar puesta al menos ${margen} días ` +
                      `antes de entrar, y aquí no llega.` };
  }

  /* 2. ¿Tiene que ser reciente? */
  if (requisito.maximoDiasAntes) {
    const dias = (dEntrada - puesta) / DIA;
    if (dias > requisito.maximoDiasAntes) {
      return { estado: "caducado", caduca: null,
               mensaje: `${requisito.nombre} tiene que ser de los ` +
                        `${requisito.maximoDiasAntes} días anteriores a la entrada.` };
    }
    return { estado: "al-dia", caduca: null, mensaje: "" };
  }

  /* 1. ¿Sigue en vigor durante toda la estancia?
     Si el propietario apuntó hasta cuándo vale, manda eso. */
  const caduca = registro.validoHasta || sumarMeses(registro.fecha, requisito.vigenciaMeses);
  const dCaduca = aFecha(caduca);
  const dSalida = aFecha(salida);

  if (dCaduca < dEntrada)
    return { estado: "caducado", caduca,
             mensaje: `${requisito.nombre} venció el ${enCristiano(caduca)}.` };

  if (dCaduca < dSalida)
    return { estado: "caduca-durante", caduca,
             mensaje: `${requisito.nombre} vence el ${enCristiano(caduca)}, ` +
                      `en mitad de la estancia.` };

  return { estado: "al-dia", caduca, mensaje: "" };
}

/** Revisa el perro entero contra unas fechas y devuelve sólo lo que falla. */
export function revisarPerro(perro, entrada, salida) {
  const problemas = [];
  for (const requisito of REQUISITOS) {
    if (!requisito.obligatorio) continue;
    const r = estadoRequisito({
      requisito, registro: perro.sanidad?.[requisito.id], entrada, salida,
    });
    if (r.estado !== "al-dia") {
      problemas.push({
        id: requisito.id,
        estado: r.estado,
        mensaje: `${perro.nombre}: ${r.mensaje}`,
      });
    }
  }
  return { apto: problemas.length === 0, problemas };
}
```

- [ ] **Paso 4: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/sanidad.test.mjs`
Esperado: PASAN las once.

- [ ] **Paso 5: commit**

```bash
git add js/sanidad.js pruebas/sanidad.test.mjs
git commit -m "Vigencia sanitaria contra las fechas de la estancia"
```

---

## Tarea 4: El esquema de la base de datos

**Ficheros:**
- Crear: `db/schema.sql`, `db/storage.sql`
- Probar: `pruebas/esquema.test.mjs`

**Interfaces:**
- Consume: nada
- Produce: tablas `cliente`, `perro`, `solicitud_cambio`; RLS activado en las tres;
  trigger `perro_chip_y_nombre_inmutables`

**Cómo se prueba esto.** No hay Postgres en el Mac de Santiago y montar Docker para él no compensa.
Las pruebas leen el SQL y comprueban que las reglas están escritas: que RLS está activado en todas
las tablas, que el trigger existe, que no hay ninguna tabla sin política. No sustituye a probarlo
contra la base real —eso se hace a mano en el panel de Supabase al final de la tarea— pero atrapa
el fallo que de verdad ocurre: **añadir una tabla y olvidarse de protegerla.**

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/esquema.test.mjs`:

```js
/* ============================================================
   El fallo que esta prueba impide: crear una tabla nueva y
   olvidarse de activarle RLS. Sin RLS, cualquiera con la clave
   pública —que va dentro de la página— lee la tabla entera.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const sql = readFileSync(new URL("../db/schema.sql", import.meta.url), "utf8");

const tablas = [...sql.matchAll(/create\s+table\s+(?:if\s+not\s+exists\s+)?(\w+)/gi)]
  .map(m => m[1].toLowerCase());

test("están las tres tablas de la fase 0", () => {
  for (const t of ["cliente", "perro", "solicitud_cambio"])
    assert.ok(tablas.includes(t), `falta la tabla ${t}`);
});

test("todas las tablas tienen RLS activado", () => {
  for (const t of tablas) {
    const re = new RegExp(`alter\\s+table\\s+${t}\\s+enable\\s+row\\s+level\\s+security`, "i");
    assert.match(sql, re, `la tabla ${t} se ha quedado sin RLS`);
  }
});

test("todas las tablas tienen al menos una política", () => {
  for (const t of tablas) {
    const re = new RegExp(`create\\s+policy[\\s\\S]{0,200}?on\\s+${t}\\b`, "i");
    assert.match(sql, re, `la tabla ${t} no tiene ninguna política`);
  }
});

test("el chip y el nombre están protegidos por trigger, no por el navegador", () => {
  assert.match(sql, /create\s+(or\s+replace\s+)?function\s+perro_chip_y_nombre_inmutables/i);
  assert.match(sql, /create\s+trigger[\s\S]{0,200}?perro_chip_y_nombre_inmutables/i);
});

test("el chip es único: el mismo perro no se da de alta dos veces", () => {
  assert.match(sql, /chip\s+text[^,]*unique/i);
});

test("no hay ni rastro de la clave de servicio ni del IBAN", () => {
  assert.doesNotMatch(sql, /service_role\s*=|ES\d{2}\s*\d{4}/i,
    "el repositorio es público: ahí no van secretos");
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/esquema.test.mjs`
Esperado: FALLA, no existe `db/schema.sql`.

- [ ] **Paso 3: escribir `db/schema.sql`**

```sql
-- ============================================================
-- AmigoMío — esquema de la fase 0: clientes y perros.
--
-- Las reglas viven aquí, no en el navegador. La clave pública
-- de Supabase va dentro de la página y cualquiera puede leerla;
-- lo único que protege los datos es lo que hay en este fichero.
-- ============================================================

-- ------------------------------------------------------------
-- Cliente. Se crea solo la primera vez que alguien entra.
-- ------------------------------------------------------------
create table if not exists cliente (
  id              uuid primary key references auth.users(id) on delete cascade,
  nombre          text not null default '',
  apellidos       text not null default '',
  dni             text not null default '',
  domicilio       text not null default '',
  telefono        text not null default '',

  -- Quién más puede recoger al perro. Lo exige el libro de registro.
  recoge_nombre   text not null default '',
  recoge_dni      text not null default '',

  -- Sólo administración pone esto.
  paga_en_persona boolean not null default false,
  es_admin        boolean not null default false,

  consiente_datos boolean not null default false,
  creado          timestamptz not null default now()
);

-- ------------------------------------------------------------
-- Perro.
-- ------------------------------------------------------------
create table if not exists perro (
  id                uuid primary key default gen_random_uuid(),
  cliente_id        uuid not null references cliente(id) on delete cascade,

  -- Chip y nombre: sólo cambian con el visto bueno de administración.
  chip              text not null unique,
  nombre            text not null,

  fecha_nacimiento  date,
  sexo              text check (sexo in ('macho','hembra')),
  castrado          boolean not null default false,
  raza              text not null default '',
  capa              text not null default '',
  estado_reproductivo text not null default '',
  foto              text,

  pautas_alimentacion text not null default '',
  cuidados            text not null default '',

  -- Cuestionario de carácter.
  agresivo_con_personas boolean not null default false,
  sociable          text not null default 'todos'
                    check (sociable in ('todos','machos','hembras','ninguno')),
  timido            boolean not null default false,
  comilon           boolean not null default false,
  polidipsia        boolean not null default false,
  destroyer         boolean not null default false,
  actividad         text not null default 'normal'
                    check (actividad in ('activo','normal','sedentario')),

  -- Perro potencialmente peligroso: licencia y seguro, por ley.
  es_ppp            boolean not null default false,
  ppp_licencia_hasta date,
  ppp_seguro_hasta   date,

  -- Fechas sanitarias. Un objeto por requisito:
  --   {"rabia": {"fecha":"2026-03-01","validoHasta":null,"primovacunacion":false}}
  -- Los identificadores son los de js/sanidad.js.
  sanidad           jsonb not null default '{}'::jsonb,

  borrador          boolean not null default false,  -- alta guardada a medias
  creado            timestamptz not null default now()
);

create index if not exists perro_del_cliente on perro (cliente_id);

-- ------------------------------------------------------------
-- Solicitudes de cambio de chip o nombre.
-- ------------------------------------------------------------
create table if not exists solicitud_cambio (
  id           uuid primary key default gen_random_uuid(),
  perro_id     uuid not null references perro(id) on delete cascade,
  cliente_id   uuid not null references cliente(id) on delete cascade,
  campo        text not null check (campo in ('chip','nombre')),
  valor_actual text not null,
  valor_nuevo  text not null,
  motivo       text not null default '',
  estado       text not null default 'pendiente'
               check (estado in ('pendiente','aprobada','rechazada')),
  resuelta_por uuid references cliente(id),
  resuelta_el  timestamptz,
  creada       timestamptz not null default now()
);

create index if not exists solicitudes_pendientes
  on solicitud_cambio (estado) where estado = 'pendiente';

-- ============================================================
-- La regla que no se negocia: el propietario NO cambia el chip
-- ni el nombre. Puede cambiar todo lo demás cuando quiera.
--
-- Esto va en trigger y no en un campo deshabilitado en pantalla,
-- porque un campo deshabilitado se salta desde el móvil en diez
-- segundos.
-- ============================================================
create or replace function perro_chip_y_nombre_inmutables()
returns trigger language plpgsql security definer as $$
declare
  quien_es_admin boolean;
begin
  select es_admin into quien_es_admin from cliente where id = auth.uid();

  if coalesce(quien_es_admin, false) then
    return new;  -- administración sí puede, tras aprobar la solicitud
  end if;

  if new.chip is distinct from old.chip then
    raise exception 'El número de chip solo se cambia con el visto bueno de AmigoMío.';
  end if;

  if new.nombre is distinct from old.nombre then
    raise exception 'El nombre solo se cambia con el visto bueno de AmigoMío.';
  end if;

  return new;
end $$;

drop trigger if exists perro_chip_y_nombre_inmutables on perro;
create trigger perro_chip_y_nombre_inmutables
  before update on perro
  for each row execute function perro_chip_y_nombre_inmutables();

-- ============================================================
-- Row Level Security.
-- ============================================================
alter table cliente          enable row level security;
alter table perro            enable row level security;
alter table solicitud_cambio enable row level security;

create or replace function es_admin()
returns boolean language sql stable security definer as $$
  select coalesce((select es_admin from cliente where id = auth.uid()), false);
$$;

-- Cliente: cada uno ve y edita lo suyo. Administración, todo.
create policy cliente_ve_lo_suyo on cliente
  for select using (id = auth.uid() or es_admin());

create policy cliente_se_crea on cliente
  for insert with check (id = auth.uid());

create policy cliente_edita_lo_suyo on cliente
  for update using (id = auth.uid() or es_admin());

-- Nadie se asciende a sí mismo: es_admin y paga_en_persona
-- se tocan desde el panel de Supabase o por un administrador.
create or replace function cliente_no_se_asciende()
returns trigger language plpgsql security definer as $$
begin
  if not es_admin() then
    new.es_admin        := old.es_admin;
    new.paga_en_persona := old.paga_en_persona;
  end if;
  return new;
end $$;

drop trigger if exists cliente_no_se_asciende on cliente;
create trigger cliente_no_se_asciende
  before update on cliente
  for each row execute function cliente_no_se_asciende();

-- Perro: el suyo, y el de cualquiera si eres administración.
create policy perro_ve_los_suyos on perro
  for select using (cliente_id = auth.uid() or es_admin());

create policy perro_da_de_alta on perro
  for insert with check (cliente_id = auth.uid() or es_admin());

create policy perro_edita_los_suyos on perro
  for update using (cliente_id = auth.uid() or es_admin());

create policy perro_borra_los_suyos on perro
  for delete using (cliente_id = auth.uid() or es_admin());

-- Solicitudes: las suyas; resolver, sólo administración.
create policy solicitud_ve_las_suyas on solicitud_cambio
  for select using (cliente_id = auth.uid() or es_admin());

create policy solicitud_la_pide_el_dueno on solicitud_cambio
  for insert with check (cliente_id = auth.uid());

create policy solicitud_la_resuelve_admin on solicitud_cambio
  for update using (es_admin());
```

- [ ] **Paso 4: escribir `db/storage.sql`**

```sql
-- ============================================================
-- Cubos de Storage.
--   fotos-perros  — la foto de la ficha
--   cartillas     — foto de la cartilla o del pasaporte europeo
--
-- Ninguno es público: las cartillas llevan datos del animal y
-- del propietario.
-- ============================================================
insert into storage.buckets (id, name, public)
values ('fotos-perros', 'fotos-perros', false),
       ('cartillas',    'cartillas',    false)
on conflict (id) do nothing;

-- Cada uno en su carpeta, que se llama como su id de usuario.
create policy "sube lo suyo" on storage.objects
  for insert to authenticated
  with check (bucket_id in ('fotos-perros','cartillas')
              and (storage.foldername(name))[1] = auth.uid()::text);

create policy "ve lo suyo" on storage.objects
  for select to authenticated
  using (bucket_id in ('fotos-perros','cartillas')
         and ((storage.foldername(name))[1] = auth.uid()::text or es_admin()));

create policy "borra lo suyo" on storage.objects
  for delete to authenticated
  using (bucket_id in ('fotos-perros','cartillas')
         and (storage.foldername(name))[1] = auth.uid()::text);
```

- [ ] **Paso 5: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/esquema.test.mjs`
Esperado: PASAN las seis.

- [ ] **Paso 6: aplicarlo en Supabase y probarlo a mano**

1. En Supabase, **SQL Editor → New query**, pegar `db/schema.sql` entero y **Run**.
2. Repetir con `db/storage.sql`.
3. En **Table Editor** comprobar que aparecen `cliente`, `perro` y `solicitud_cambio` con el
   candado de RLS.
4. Prueba de verdad del trigger: crear un cliente y un perro de mentira desde el SQL Editor,
   intentar `update perro set chip = '...'` como usuario normal y **comprobar que salta el error**.
   Ese es el momento en que se sabe si la regla funciona.

- [ ] **Paso 7: commit**

```bash
git add db/ pruebas/esquema.test.mjs
git commit -m "Esquema de clientes y perros, con RLS y el chip protegido"
```

---

## Tarea 5: Entrada con correo y contraseña

**Ficheros:**
- Crear: `js/sesion.js`
- Modificar: `index.html` (cargar la librería de Supabase), `js/app.js`
- Probar: `pruebas/sesion.test.mjs`

**Interfaces:**
- Consume: `CONFIG.SUPABASE_URL`, `CONFIG.SUPABASE_ANON` de `js/config.js`
- Produce:
  - `darseDeAlta(correo, contrasena) -> {ok, mensaje}`
  - `entrar(correo, contrasena) -> {ok, mensaje}`
  - `salir()`
  - `sesionActual() -> {usuario|null, correoVerificado: boolean}`
  - `puedeReservar(sesion) -> boolean`

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/sesion.test.mjs`:

```js
/* ============================================================
   El registro es abierto: cualquiera se da de alta sin invitación.
   Por eso el correo verificado es la única barrera antes de poder
   reservar. Sin ella, cualquiera con un correo inventado retiene
   un alojamiento 24 horas en Semana Santa y desaparece.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { puedeReservar } from "../js/sesion.js";

const lee = f => readFileSync(new URL("../" + f, import.meta.url), "utf8");

test("sin sesión no se reserva", () => {
  assert.equal(puedeReservar(null), false);
  assert.equal(puedeReservar({ usuario: null, correoVerificado: false }), false);
});

test("con sesión pero sin verificar el correo, tampoco", () => {
  assert.equal(puedeReservar({ usuario: { id: "x" }, correoVerificado: false }), false);
});

test("con el correo verificado, sí", () => {
  assert.equal(puedeReservar({ usuario: { id: "x" }, correoVerificado: true }), true);
});

test("la contraseña no se guarda ni se registra en ningún sitio", () => {
  const s = lee("js/sesion.js");
  assert.doesNotMatch(s, /localStorage\.setItem\([^)]*contrasena/i);
  assert.doesNotMatch(s, /console\.log\([^)]*contrasena/i);
});

test("los mensajes de error hablan como una persona", () => {
  const s = lee("js/sesion.js");
  assert.doesNotMatch(s, /Invalid login credentials/,
    "hay que traducir los mensajes de Supabase, no soltárselos al cliente");
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/sesion.test.mjs`
Esperado: FALLA, no existe `js/sesion.js`.

- [ ] **Paso 3: cargar la librería de Supabase en `index.html`**

Añadir antes de `js/config.js`:

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.45.4/dist/umd/supabase.min.js"></script>
```

Versión clavada a propósito: sin `@latest`, para que una actualización suya no rompa la app un
martes cualquiera.

- [ ] **Paso 4: escribir `js/sesion.js`**

```js
/* ============================================================
   Entrada con correo y contraseña. El registro es abierto: no
   hace falta invitación.

   Supabase guarda las contraseñas cifradas. Aquí no se guarda
   ninguna, ni se escribe en el registro de la consola.
   ============================================================ */

export const supabase = window.supabase?.createClient(
  window.CONFIG.SUPABASE_URL, window.CONFIG.SUPABASE_ANON
);

/* Los mensajes de Supabase vienen en inglés y de sistema.
   Al cliente se le habla en cristiano. */
const TRADUCCIONES = [
  [/Invalid login credentials/i,        "Ese correo y esa contraseña no cuadran. Prueba otra vez."],
  [/Email not confirmed/i,              "Todavía no has confirmado el correo. Mira tu bandeja."],
  [/User already registered/i,          "Ese correo ya está dado de alta. Prueba a entrar."],
  [/Password should be at least/i,      "La contraseña se queda corta: mínimo 8 caracteres."],
  [/rate limit|too many requests/i,     "Has probado muchas veces seguidas. Espera un minuto."],
];

function enCristiano(error) {
  if (!error) return "";
  for (const [re, texto] of TRADUCCIONES) if (re.test(error.message)) return texto;
  return "Se nos ha atragantado algo. Inténtalo en un momento.";
}

export async function darseDeAlta(correo, contrasena) {
  const { error } = await supabase.auth.signUp({
    email: correo,
    password: contrasena,
    options: { emailRedirectTo: location.origin + location.pathname },
  });
  if (error) return { ok: false, mensaje: enCristiano(error) };
  return { ok: true,
           mensaje: "¡Hecho! Te hemos mandado un correo para confirmar. " +
                    "Pincha el enlace y ya estás dentro." };
}

export async function entrar(correo, contrasena) {
  const { error } = await supabase.auth.signInWithPassword({ email: correo, password: contrasena });
  if (error) return { ok: false, mensaje: enCristiano(error) };
  return { ok: true, mensaje: "" };
}

export async function salir() {
  await supabase.auth.signOut();
}

export async function sesionActual() {
  const { data } = await supabase.auth.getSession();
  const usuario = data?.session?.user || null;
  return { usuario, correoVerificado: !!usuario?.email_confirmed_at };
}

/* La barrera: registro abierto, pero sin correo verificado no se reserva. */
export function puedeReservar(sesion) {
  return !!(sesion && sesion.usuario && sesion.correoVerificado);
}
```

- [ ] **Paso 5: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/sesion.test.mjs`
Esperado: PASAN las cinco.

- [ ] **Paso 6: probarlo de verdad en el navegador**

`npm run servir`, darse de alta con un correo real, comprobar que llega el correo de confirmación,
pincharlo y comprobar que se entra. Es la única forma de saber que el `emailRedirectTo` está bien.

- [ ] **Paso 7: commit**

```bash
git add js/sesion.js pruebas/sesion.test.mjs index.html
git commit -m "Entrada con correo y contraseña, con el correo verificado como barrera"
```

---

## Tarea 6: Alta de perro en pasos, con guardado a medias

**Ficheros:**
- Crear: `js/datos.js`, `js/formularios.js`, `js/vistas/perros.js`
- Modificar: `js/app.js`
- Probar: `pruebas/alta-perro.test.mjs`

**Interfaces:**
- Consume: `chipValido`, `normalizarChip` de `js/perro.js`; `REQUISITOS` de `js/sanidad.js`;
  `supabase` de `js/sesion.js`
- Produce:
  - `PASOS` — los pasos del alta, con sus campos
  - `validarPaso(numeroPaso, datos) -> {ok, faltan: [{campo, mensaje}]}`
  - `guardarPerro(datos, {borrador}) -> {ok, id, mensaje}`
  - `misPerros() -> [perro]`

**Por qué en pasos:** es un formulario largo y de una sentada la gente lo abandona. Tres pantallas
cortas —quién es, cómo come, cómo es— y se puede dejar a medias y seguir mañana. Eso es lo que hace
el campo `borrador` de la tabla.

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/alta-perro.test.mjs`:

```js
/* ============================================================
   El alta del perro es el formulario más largo de la app.
   Si se suelta de golpe, la gente lo abandona a la mitad.
   Va en tres pasos y se puede guardar a medias.

   Y los mensajes de lo que falta se le enseñan tal cual al
   cliente: por eso se comprueba también cómo están escritos.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { PASOS, validarPaso } from "../js/formularios.js";

test("el alta va en tres pasos cortos", () => {
  assert.equal(PASOS.length, 3);
  assert.deepEqual(PASOS.map(p => p.id), ["quien-es", "como-come", "como-es"]);
});

test("el paso 1 exige chip, nombre y fecha de nacimiento", () => {
  const r = validarPaso(0, {});
  assert.equal(r.ok, false);
  const campos = r.faltan.map(f => f.campo);
  for (const c of ["chip", "nombre", "fecha_nacimiento", "sexo"])
    assert.ok(campos.includes(c), `debería pedir ${c}`);
});

test("un chip mal formado se rechaza con su explicación", () => {
  const r = validarPaso(0, { chip: "12345", nombre: "Luna",
                             fecha_nacimiento: "2022-04-01", sexo: "hembra" });
  assert.equal(r.ok, false);
  const chip = r.faltan.find(f => f.campo === "chip");
  assert.match(chip.mensaje, /15/, "hay que decirle que son 15 dígitos");
});

test("el paso 1 pasa con todo relleno", () => {
  const r = validarPaso(0, { chip: "941 0000 1234 5678", nombre: "Luna",
                             fecha_nacimiento: "2022-04-01", sexo: "hembra" });
  assert.equal(r.ok, true);
  assert.equal(r.faltan.length, 0);
});

test("un perro potencialmente peligroso necesita licencia y seguro", () => {
  const r = validarPaso(2, { es_ppp: true, sociable: "todos", actividad: "normal" });
  assert.equal(r.ok, false);
  const campos = r.faltan.map(f => f.campo);
  assert.ok(campos.includes("ppp_licencia_hasta"));
  assert.ok(campos.includes("ppp_seguro_hasta"));
});

test("los mensajes hablan como una persona, no como un formulario", () => {
  const r = validarPaso(0, {});
  for (const f of r.faltan) {
    assert.doesNotMatch(f.mensaje, /obligatorio|inválido|error/i,
      `"${f.mensaje}" suena a máquina`);
  }
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/alta-perro.test.mjs`
Esperado: FALLA, no existe `js/formularios.js`.

- [ ] **Paso 3: escribir `js/formularios.js`**

```js
/* ============================================================
   El alta del perro, en tres pasos cortos.

   Todo lo de aquí es lógica pura: recibe lo que hay escrito en
   el formulario y dice qué falta. No toca el navegador, para
   poder probarlo.
   ============================================================ */
import { chipValido } from "./perro.js";

export const PASOS = [
  { id: "quien-es",  titulo: "¿Quién es?",
    campos: ["chip", "nombre", "fecha_nacimiento", "sexo", "raza", "capa", "castrado", "foto"] },
  { id: "como-come", titulo: "¿Cómo come y qué cuidados necesita?",
    campos: ["pautas_alimentacion", "cuidados", "sanidad"] },
  { id: "como-es",   titulo: "¿Cómo es?",
    campos: ["agresivo_con_personas", "sociable", "timido", "comilon",
             "polidipsia", "destroyer", "actividad", "es_ppp",
             "ppp_licencia_hasta", "ppp_seguro_hasta"] },
];

const vacio = v => v === undefined || v === null || String(v).trim() === "";

export function validarPaso(numeroPaso, datos) {
  const faltan = [];
  const falta = (campo, mensaje) => faltan.push({ campo, mensaje });

  if (numeroPaso === 0) {
    if (vacio(datos.nombre))           falta("nombre", "¿Cómo se llama?");
    if (vacio(datos.chip))             falta("chip", "Nos falta el número de chip, el de la cartilla.");
    else if (!chipValido(datos.chip))  falta("chip", "Ese chip no cuadra: son 15 dígitos seguidos.");
    if (vacio(datos.fecha_nacimiento)) falta("fecha_nacimiento", "¿Cuándo nació? Si no lo sabes exacto, aproxima.");
    if (vacio(datos.sexo))             falta("sexo", "¿Es macho o hembra?");
  }

  if (numeroPaso === 2) {
    if (vacio(datos.sociable))  falta("sociable", "¿Cómo se lleva con otros perros?");
    if (vacio(datos.actividad)) falta("actividad", "¿Es de moverse mucho o de tumbarse al sol?");
    if (datos.es_ppp === true) {
      if (vacio(datos.ppp_licencia_hasta))
        falta("ppp_licencia_hasta", "Como es un perro potencialmente peligroso, nos hace falta hasta cuándo vale tu licencia.");
      if (vacio(datos.ppp_seguro_hasta))
        falta("ppp_seguro_hasta", "Y hasta cuándo vale el seguro de responsabilidad civil.");
    }
  }

  return { ok: faltan.length === 0, faltan };
}
```

- [ ] **Paso 4: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/alta-perro.test.mjs`
Esperado: PASAN las seis.

- [ ] **Paso 5: escribir `js/datos.js`**

```js
/* ============================================================
   Todo lo que habla con Supabase pasa por aquí. Así, el día que
   haya que cambiar cómo se guardan las cosas, se toca un fichero
   y no doce.
   ============================================================ */
import { supabase } from "./sesion.js";
import { normalizarChip } from "./perro.js";

export async function miFicha() {
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return null;
  let { data } = await supabase.from("cliente").select("*").eq("id", user.id).maybeSingle();
  if (!data) {
    /* Primera vez que entra: se crea su ficha sola. */
    const { data: nueva } = await supabase.from("cliente")
      .insert({ id: user.id }).select().single();
    data = nueva;
  }
  return data;
}

export async function misPerros() {
  const { data, error } = await supabase.from("perro")
    .select("*").order("nombre");
  if (error) throw error;
  return data || [];
}

export async function guardarPerro(datos, { borrador = false } = {}) {
  const { data: { user } } = await supabase.auth.getUser();
  const fila = { ...datos, cliente_id: user.id, borrador,
                 chip: normalizarChip(datos.chip || "") };

  if (datos.id) {
    /* Chip y nombre no se mandan en las modificaciones: los rechaza
       el trigger y además no queremos ni intentarlo. */
    const { id, chip, nombre, ...resto } = fila;
    const { error } = await supabase.from("perro").update(resto).eq("id", datos.id);
    if (error) return { ok: false, mensaje: mensajeDeError(error) };
    return { ok: true, id: datos.id, mensaje: "Guardado." };
  }

  const { data, error } = await supabase.from("perro").insert(fila).select("id").single();
  if (error) return { ok: false, mensaje: mensajeDeError(error) };
  return { ok: true, id: data.id, mensaje: "" };
}

function mensajeDeError(error) {
  if (/duplicate key.*chip/i.test(error.message))
    return "Ese chip ya está dado de alta. Si es tu perro y no lo ves, avísanos.";
  if (/chip solo se cambia|nombre solo se cambia/i.test(error.message))
    return error.message;  // ya viene escrito para el cliente
  return "No hemos podido guardarlo. Inténtalo en un momento.";
}

export async function pedirCambio({ perroId, campo, valorActual, valorNuevo, motivo }) {
  const { data: { user } } = await supabase.auth.getUser();
  const { error } = await supabase.from("solicitud_cambio").insert({
    perro_id: perroId, cliente_id: user.id, campo,
    valor_actual: valorActual, valor_nuevo: valorNuevo, motivo,
  });
  if (error) return { ok: false, mensaje: "No hemos podido enviar la solicitud." };
  return { ok: true, mensaje: "Recibido. Lo miramos y te decimos algo." };
}
```

- [ ] **Paso 6: escribir `js/vistas/perros.js` y engancharlo en `js/app.js`**

La pantalla: lista de perros con su foto y su nombre, botón de «Dar de alta un perro», y el
formulario en tres pasos con «Guardar y seguir luego» en cada uno. Los mensajes de `validarPaso`
se pintan debajo de su campo.

Comprobar a mano: dar de alta un perro, salir a mitad del paso 2, volver a entrar y comprobar que
sigue ahí como borrador.

- [ ] **Paso 7: commit**

```bash
git add js/datos.js js/formularios.js js/vistas/perros.js js/app.js pruebas/alta-perro.test.mjs
git commit -m "Alta de perro en tres pasos, con guardado a medias"
```

---

## Tarea 7: Mi ficha, consentimiento y fechas sanitarias

Esta tarea tapa dos cosas que el diseño exige y que sin ella se quedan sólo en la tabla: los datos
del cliente que pide el libro de registro, y la pantalla donde se meten las fechas de vacunas.

**Ficheros:**
- Crear: `js/vistas/mi-ficha.js`, `js/vistas/sanidad.js`
- Modificar: `js/datos.js`, `js/app.js`
- Probar: `pruebas/mi-ficha.test.mjs`

**Interfaces:**
- Consume: `miFicha` de `js/datos.js`; `REQUISITOS`, `estadoRequisito` de `js/sanidad.js`
- Produce:
  - `validarFichaCliente(datos) -> {ok, faltan: [{campo, mensaje}]}`
  - `guardarMiFicha(datos) -> {ok, mensaje}`
  - `camposSanidad(perro) -> [{id, nombre, obligatorio, fecha, validoHasta, primovacunacion}]`

**Por qué el consentimiento va aquí y no en el alta de la cuenta:** en el alta sólo hay correo y
contraseña, que es dato de la propia cuenta. El consentimiento hace falta cuando se guardan datos
personales de verdad —DNI, domicilio, datos veterinarios del animal— y ese momento es éste.

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/mi-ficha.test.mjs`:

```js
/* ============================================================
   Los datos del cliente no son un capricho de formulario: el
   libro de registro del núcleo zoológico exige nombre, DNI,
   domicilio, teléfono y quién está autorizado a recoger al
   perro. Sin eso, el libro nace incompleto.

   Y el consentimiento de protección de datos es obligatorio en
   cuanto se guarda un DNI.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { validarFichaCliente, camposSanidad } from "../js/vistas/mi-ficha.js";

test("la ficha exige lo que pide el libro de registro", () => {
  const r = validarFichaCliente({});
  assert.equal(r.ok, false);
  const campos = r.faltan.map(f => f.campo);
  for (const c of ["nombre", "apellidos", "dni", "domicilio", "telefono"])
    assert.ok(campos.includes(c), `el libro de registro pide ${c}`);
});

test("sin consentimiento no se guarda nada", () => {
  const r = validarFichaCliente({
    nombre: "Santiago", apellidos: "Díaz", dni: "12345678Z",
    domicilio: "Calle Mayor 1", telefono: "600000000",
    consiente_datos: false,
  });
  assert.equal(r.ok, false);
  assert.ok(r.faltan.some(f => f.campo === "consiente_datos"));
});

test("con todo relleno y consentido, pasa", () => {
  const r = validarFichaCliente({
    nombre: "Santiago", apellidos: "Díaz", dni: "12345678Z",
    domicilio: "Calle Mayor 1", telefono: "600000000",
    consiente_datos: true,
  });
  assert.equal(r.ok, true);
});

test("la persona autorizada a recoger es opcional, pero si va el nombre va el DNI", () => {
  const base = { nombre: "S", apellidos: "D", dni: "1", domicilio: "c", telefono: "6",
                 consiente_datos: true };
  assert.equal(validarFichaCliente(base).ok, true, "sin nadie autorizado, vale");
  const r = validarFichaCliente({ ...base, recoge_nombre: "María" });
  assert.equal(r.ok, false);
  assert.ok(r.faltan.some(f => f.campo === "recoge_dni"),
    "hay que comprobar su identidad en la entrega, así que el DNI hace falta");
});

test("la pantalla de sanidad ofrece un hueco por requisito", () => {
  const campos = camposSanidad({ sanidad: { rabia: { fecha: "2026-03-01" } } });
  const ids = campos.map(c => c.id);
  for (const id of ["rabia", "polivalente", "leptospirosis", "traqueobronquitis",
                    "desparasitacion_interna", "antiparasitario_externo", "leishmaniosis"])
    assert.ok(ids.includes(id), `falta el hueco de ${id}`);

  const rabia = campos.find(c => c.id === "rabia");
  assert.equal(rabia.fecha, "2026-03-01", "lo ya guardado viene relleno");
  assert.equal(rabia.obligatorio, true);

  const leish = campos.find(c => c.id === "leishmaniosis");
  assert.equal(leish.obligatorio, false, "la leishmaniosis se recomienda, no se exige");
  assert.equal(leish.fecha, "", "lo que no hay, vacío, no undefined");
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/mi-ficha.test.mjs`
Esperado: FALLA, no existe `js/vistas/mi-ficha.js`.

- [ ] **Paso 3: escribir la lógica de `js/vistas/mi-ficha.js`**

```js
/* ============================================================
   Mi ficha: los datos del cliente y las fechas sanitarias del
   perro.

   Lo que se pide aquí no lo elegimos nosotros: sale del libro de
   registro que exige el programa sanitario del núcleo zoológico.
   Por eso el DNI y el domicilio no son opcionales.
   ============================================================ */
import { REQUISITOS } from "../sanidad.js";

const vacio = v => v === undefined || v === null || String(v).trim() === "";

export function validarFichaCliente(datos) {
  const faltan = [];
  const falta = (campo, mensaje) => faltan.push({ campo, mensaje });

  if (vacio(datos.nombre))    falta("nombre", "¿Cómo te llamas?");
  if (vacio(datos.apellidos)) falta("apellidos", "Y tus apellidos.");
  if (vacio(datos.dni))       falta("dni", "Nos hace falta tu DNI para el libro de registro.");
  if (vacio(datos.domicilio)) falta("domicilio", "Tu dirección, también para el libro.");
  if (vacio(datos.telefono))  falta("telefono", "Un teléfono por si pasa algo mientras está aquí.");

  /* Si hay alguien autorizado a recoger, en la entrega hay que
     comprobarle el DNI. Sin DNI, esa autorización no sirve. */
  if (!vacio(datos.recoge_nombre) && vacio(datos.recoge_dni))
    falta("recoge_dni", "Nos falta el DNI de quien puede recoger al perro: se lo pedimos al entregarlo.");

  if (datos.consiente_datos !== true)
    falta("consiente_datos", "Necesitamos que aceptes cómo guardamos tus datos.");

  return { ok: faltan.length === 0, faltan };
}

/** Un hueco por requisito, con lo ya guardado relleno. */
export function camposSanidad(perro) {
  const guardado = perro?.sanidad || {};
  return REQUISITOS.map(r => {
    const v = guardado[r.id] || {};
    return {
      id: r.id,
      nombre: r.nombre,
      obligatorio: r.obligatorio,
      fecha: v.fecha || "",
      validoHasta: v.validoHasta || "",
      primovacunacion: v.primovacunacion === true,
    };
  });
}
```

- [ ] **Paso 4: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/mi-ficha.test.mjs`
Esperado: PASAN las cinco.

- [ ] **Paso 5: añadir `guardarMiFicha` a `js/datos.js`**

```js
export async function guardarMiFicha(datos) {
  const { data: { user } } = await supabase.auth.getUser();
  /* es_admin y paga_en_persona no se mandan nunca desde aquí:
     los devuelve a su sitio el trigger cliente_no_se_asciende,
     pero mejor ni intentarlo. */
  const { es_admin, paga_en_persona, id, ...resto } = datos;
  const { error } = await supabase.from("cliente").update(resto).eq("id", user.id);
  if (error) return { ok: false, mensaje: "No hemos podido guardar tus datos." };
  return { ok: true, mensaje: "Guardado." };
}
```

- [ ] **Paso 6: pintar las dos pantallas**

`mi-ficha.js` dibuja el formulario del cliente con los mensajes de `validarFichaCliente` debajo de
cada campo, y la casilla de consentimiento con enlace a la política de privacidad.

`sanidad.js` dibuja una fila por cada elemento de `camposSanidad(perro)`: nombre del requisito,
fecha, «válido hasta» opcional, casilla de primovacunación, y un botón para adjuntar foto de la
cartilla al cubo `cartillas`. Los no obligatorios van al final, bajo el rótulo «Recomendadas».

**Patrón de pintado, el mismo en todas las vistas del proyecto:** cada vista exporta
`render(contenedor, estado)` y no toca nada fuera de su contenedor. Sin plantillas ni framework:
`innerHTML` con literales de plantilla, y los eventos enganchados con `addEventListener` después
de pintar.

- [ ] **Paso 7: comprobarlo a mano**

Rellenar la ficha sin marcar el consentimiento y comprobar que no deja · marcarlo y guardar ·
meter la rabia con fecha de hace dos años y comprobar que, al pasar por `estadoRequisito` con unas
fechas de estancia cualquiera, sale «caducado».

- [ ] **Paso 8: commit**

```bash
git add js/vistas/mi-ficha.js js/vistas/sanidad.js js/datos.js pruebas/mi-ficha.test.mjs
git commit -m "Mi ficha con los datos del libro de registro y las fechas sanitarias"
```

---

## Tarea 8: Solicitudes de cambio y panel de administración

**Ficheros:**
- Crear: `js/vistas/admin-clientes.js`, `js/vistas/admin-solicitudes.js`
- Modificar: `js/datos.js`, `js/app.js`
- Probar: `pruebas/admin.test.mjs`

**Interfaces:**
- Consume: `pedirCambio`, `miFicha` de `js/datos.js`
- Produce:
  - `solicitudesPendientes() -> [solicitud]`
  - `resolverSolicitud(id, {aprobar: boolean}) -> {ok, mensaje}`
  - `clientes(busqueda) -> [cliente]`
  - `autorizarPagoEnPersona(clienteId, valor) -> {ok}`

- [ ] **Paso 1: escribir la prueba que va a fallar**

`pruebas/admin.test.mjs`:

```js
/* ============================================================
   Aprobar una solicitud de cambio de chip tiene que hacer DOS
   cosas: marcar la solicitud como aprobada y cambiar el chip
   de verdad. Si sólo hace la primera, administración cree que
   lo ha arreglado y el perro sigue con el chip viejo.
   ============================================================ */
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const datos = readFileSync(new URL("../js/datos.js", import.meta.url), "utf8");

test("aprobar una solicitud también cambia el dato en el perro", () => {
  const i = datos.indexOf("resolverSolicitud");
  assert.notEqual(i, -1, "falta resolverSolicitud");
  const cuerpo = datos.slice(i, i + 1400);
  assert.match(cuerpo, /from\("perro"\)[\s\S]{0,120}update/,
    "aprobar tiene que escribir el valor nuevo en la tabla perro");
  assert.match(cuerpo, /estado:\s*["']aprobada["']/,
    "y marcar la solicitud como aprobada");
});

test("rechazar no toca el perro", () => {
  const i = datos.indexOf("resolverSolicitud");
  const cuerpo = datos.slice(i, i + 1400);
  assert.match(cuerpo, /aprobar\s*\?|if\s*\(\s*aprobar/,
    "tiene que distinguir aprobar de rechazar");
});

test("el registro de quién resolvió qué no se pierde", () => {
  const i = datos.indexOf("resolverSolicitud");
  const cuerpo = datos.slice(i, i + 1400);
  assert.match(cuerpo, /resuelta_por/);
  assert.match(cuerpo, /resuelta_el/);
});
```

- [ ] **Paso 2: ejecutarla para ver que falla**

Ejecutar: `node --test pruebas/admin.test.mjs`
Esperado: FALLA, `resolverSolicitud` no existe.

- [ ] **Paso 3: añadir a `js/datos.js`**

```js
export async function solicitudesPendientes() {
  const { data, error } = await supabase.from("solicitud_cambio")
    .select("*, perro(nombre, chip), cliente(nombre, apellidos, telefono)")
    .eq("estado", "pendiente").order("creada");
  if (error) throw error;
  return data || [];
}

/**
 * Aprobar NO es sólo marcar la solicitud: hay que escribir el valor
 * nuevo en el perro. Si se hace sólo lo primero, administración cree
 * que lo ha resuelto y el perro sigue igual.
 */
export async function resolverSolicitud(id, { aprobar }) {
  const { data: { user } } = await supabase.auth.getUser();
  const { data: solicitud, error: e1 } = await supabase
    .from("solicitud_cambio").select("*").eq("id", id).single();
  if (e1) return { ok: false, mensaje: "No encontramos esa solicitud." };

  if (aprobar) {
    const { error: e2 } = await supabase.from("perro")
      .update({ [solicitud.campo]: solicitud.valor_nuevo })
      .eq("id", solicitud.perro_id);
    if (e2) return { ok: false, mensaje: "No hemos podido cambiar el dato del perro." };
  }

  const { error: e3 } = await supabase.from("solicitud_cambio").update({
    estado: aprobar ? "aprobada" : "rechazada",
    resuelta_por: user.id,
    resuelta_el: new Date().toISOString(),
  }).eq("id", id);
  if (e3) return { ok: false, mensaje: "El dato se cambió, pero no pudimos cerrar la solicitud." };

  return { ok: true, mensaje: aprobar ? "Aprobada y aplicada." : "Rechazada." };
}

export async function clientes(busqueda = "") {
  let q = supabase.from("cliente").select("*").order("apellidos");
  if (busqueda) q = q.or(`nombre.ilike.%${busqueda}%,apellidos.ilike.%${busqueda}%,telefono.ilike.%${busqueda}%`);
  const { data, error } = await q;
  if (error) throw error;
  return data || [];
}

export async function autorizarPagoEnPersona(clienteId, valor) {
  const { error } = await supabase.from("cliente")
    .update({ paga_en_persona: valor }).eq("id", clienteId);
  return { ok: !error };
}
```

- [ ] **Paso 4: ejecutar las pruebas y comprobar que pasan**

Ejecutar: `node --test pruebas/admin.test.mjs`
Esperado: PASAN las tres.

- [ ] **Paso 5: escribir las dos pantallas de administración**

`js/vistas/admin-solicitudes.js`: lista de pendientes con el valor viejo, el nuevo, el motivo y dos
botones. `js/vistas/admin-clientes.js`: buscador, ficha editable y el interruptor de pago en persona.

Ambas sólo aparecen en el menú si `miFicha().es_admin` es cierto. **Eso es sólo para no enseñar
botones inútiles**: quien protege de verdad es RLS, que ya rechaza la operación aunque alguien
llegue a la pantalla.

- [ ] **Paso 6: comprobarlo a mano**

Marcar a Santiago como administrador desde el panel de Supabase
(`update cliente set es_admin = true where id = '...'`), pedir un cambio de chip desde una cuenta
normal, aprobarlo desde la de administración y **comprobar en Table Editor que el chip del perro
cambió de verdad**.

- [ ] **Paso 7: commit**

```bash
git add js/vistas/ js/datos.js pruebas/admin.test.mjs
git commit -m "Solicitudes de cambio y panel de administración de clientes"
```

---

## Tarea 9: CLAUDE.md y cierre de la fase

**Ficheros:**
- Crear: `CLAUDE.md`
- Probar: ejecutar todo

- [ ] **Paso 1: escribir `CLAUDE.md`**

Siguiendo el del CEPPB: qué es AmigoMío, quién es el interlocutor y cómo hablarle, la pila técnica,
las reglas que no se negocian (chip y nombre, RLS, nada de secretos en el repositorio público,
tono familiar), y las decisiones que no se deducen del código, con enlace al documento de diseño.

- [ ] **Paso 2: ejecutar todas las pruebas**

Ejecutar: `npm test`
Esperado: pasan todas, sin ninguna saltada.

- [ ] **Paso 3: repaso a mano de la fase entera**

Darse de alta con un correo nuevo · confirmarlo · dar de alta un perro dejándolo a medias ·
volver y terminarlo · intentar cambiarle el chip y comprobar que no deja · pedir el cambio ·
aprobarlo desde administración · comprobar que el chip cambió.

- [ ] **Paso 4: commit**

```bash
git add CLAUDE.md
git commit -m "Documentación del proyecto y cierre de la fase 0"
```

---

## Lo que esta fase NO hace

Para que nadie lo busque: no hay reservas, ni alojamientos, ni tarifas, ni festivos, ni
disponibilidad, ni justificantes, ni correos automáticos, ni el cuadro de ocupación, ni la hoja
del día, ni los libros de registro, ni Zapatilla. Todo eso son las fases 1 a 4.

Al terminar la fase 0 hay clientes que entran, perros con su ficha completa y sus fechas
sanitarias, el chip protegido de verdad, y un panel desde el que Santiago gobierna todo eso.
